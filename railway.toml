# Railway Configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python update_server_production.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
FLASK_ENV = "production"
LOG_LEVEL = "INFO"
HOST = "0.0.0.0"